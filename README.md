# Movie-Sentiment-Classifer
A machine learning project that classifies movie reviews as positive or negative using an LSTM-based deep learning model trained on the IMDB dataset. The project includes full text preprocessing, model training, evaluation, and prediction pipeline.
